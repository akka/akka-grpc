akka.http {
    client.http2 {
        log-frames = true
    }
    server {
        default-host-header = "localhost.com"
        preview.enable-http2 = on
        remote-address-attribute = on
    }
}