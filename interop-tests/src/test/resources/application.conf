akka.http {
    client.http2 {
        log-frames = true
        max-persistent-attempts = 10
    }
    server {
        default-host-header = "localhost.com"
        preview.enable-http2 = on
    }
}