name: Setup global resolver for Akka

runs:
  using: composite
  steps:
    - name: Checkout Global Scripts
      uses: actions/checkout@v6
      with:
        repository: akka/github-actions-scripts
        path: github-actions-scripts

    - name: Setup global resolver
      run: ./github-actions-scripts/setup_global_resolver.sh sbt-plugin
      shell: bash